import { MyContainer, UpdateProfile as UpdateProfileC } from "../../components";

const UpdateProfile = () => {
  return (
    <MyContainer>
      <UpdateProfileC />
    </MyContainer>
  );
};

export default UpdateProfile;
