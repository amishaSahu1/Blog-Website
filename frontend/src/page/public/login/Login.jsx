import { LoginForm, MetaData, MyContainer } from "../../../components";

const Login = () => {
  return (
    <MyContainer>
      <MetaData title={`Login`} />
      <LoginForm />
    </MyContainer>
  );
};

export default Login;
